# Firebase Studio

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.

Para **PWA (offline)** y **despliegue** (build estático `out/` o hosting con Node), ver [docs/DEPLOY.md](docs/DEPLOY.md).
